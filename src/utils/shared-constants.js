export const address = 'ws://localhost:9944'; // Websocket address of your Dock node
export const secretUri = '//Alice'; // Account secret in uri format, we will use Alice for local testing
export const secretUri2 = '//Bob'; // Account secret in uri format, we will use Alice for local testing
export const secretUri3 = '//Bob2'; // Account secret in uri format, we will use Alice for local testing
export const secretUri4 = '//Bob3'; // Account secret in uri format, we will use Alice for local testing
